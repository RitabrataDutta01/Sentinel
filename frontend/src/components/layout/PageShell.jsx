/**
 * Backwards-compatible alias — the app shell now lives in AppShell.jsx
 * (sidebar + ambient texture). All pages import this as PageShell.
 */
import AppShell from './AppShell'
export default AppShell
